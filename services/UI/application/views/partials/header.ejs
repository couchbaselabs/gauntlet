<!DOCTYPE html>
<html lang="en">
<head>
  <title> E2E App - <%= page %> </title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!--link rel="stylesheet" type="text/css" href="/bootstrap_css/bootstrap.min.css"-->
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">

  <script type="text/javascript" src="/jquery/jquery.min.js"></script>
  <script type="text/javascript" src="/bootstrap_js/bootstrap.bundle.min.js"></script>

  <!--script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script-->
  <!--script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script-->

  <style>
    /* Remove the navbar's default margin-bottom and rounded borders */
    .navbar {
      margin-bottom: 0;
      border-radius: 0;
    }

    /* Set height of the grid so .sidenav can be 100% (adjust as needed) */
    .row.content {height: 450px}

    /* Set gray background color and 100% height */
    .sidenav {
      padding-top: 20px;
      background-color: #fff;
      height: 100%;
    }

    /* Set black background color, white text and some padding */
    footer {
      background-color: #555;
      color: white;
      padding: 15px;
    }

    /* On small screens, set height to 'auto' for sidenav and grid */
    @media screen and (max-width: 767px) {
      .sidenav {
        height: auto;
        padding: 15px;
      }
      .row.content {height:auto;}
    }
  </style>
</head>
<body>

<%
data = [{ text: "Flights", href: "/flight_search", img: "/icons/search.svg" },
        { text: "Booking history", href: "/booking_history", img: "/icons/ticket-detailed.svg" },
        { text: "Cancel Booking", href: "/cancel_booking", img: "/icons/x-octagon.svg"}
       ];
%>
<nav class="navbar navbar-dark bg-dark">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#"><img src="logo.svg" width="90em" height="25em"></a>
    </div>
    <div class="collapse navbar-collapse" id="myNavbar">
      <ul class="nav navbar-nav">
        <% for(i=0; i < data.length; i++) { %>
          <li>
          <a href="<%=data[i].href%>"><%=data[i].text%>
            <img src="<%=data[i].img%>" alt="" width="20em" height="20em" title="">
          </a>
          </li>
        <% } %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li>
        <% if(!username) { %>
          <a data-toggle="tooltip" data-placement="left" title="Login" href="/login">
            <span class="glyphicon glyphicon-log-in"></span>
            <img src="/icons/person.svg"> Login
          </a>
        <% } else { %>
          <a data-toggle="tooltip" data-placement="left" title="Hello <%=username%>"href="/logout">
            <span class="glyphicon glyphicon-log-out"></span>
            <img src="/icons/person-fill.svg"> Logout
          </a>
        <% } %>
        </li>
      </ul>
    </div>
  </div>
</nav>

<div class="container-fluid text-center">
  <div class="row content">
    <div class="col-sm-2 sidenav">
    </div>
    <div class="col-sm-8 text-left" height="100%">
